// Another Jailbreak — global settings (all maps)

"Settings"
{
	"freeday_warden_only_damage"    "1"
	"freeday_trail"                 "1"

	// Map name prefixes that enable AJB, space-separated. A map activates if it starts
	// with ANY of these. Leave empty to disable prefix matching (sm_ajb_force 1 still forces on).
	"map_prefixes"                  "jb_ jail_"
}
